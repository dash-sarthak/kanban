version: "2"
sql:
- schema: "sql/schema"
  queries: "sql/queries"
  engine: "postgresql"
  gen:
    go:
        package: "kanban"
        out: "internal/database"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
